#include <stdio.h>

int main()
{
	int a = 65;

  // The %d is a placeholder for another argument that must be added, in this
  // case it is the variable a.
	printf("The value of 'a' is %d\n",a);
	return 0;
}
